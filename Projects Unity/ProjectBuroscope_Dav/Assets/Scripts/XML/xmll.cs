using UnityEngine;
using System.Collections;

public class xmll : MonoBehaviour {

    public Transform xmlll;

	void Update () {
        //GetComponent<TextMesh>().text = xmlll.GetComponent<XmlReader>().getXmlValue(0,0, "titre", 0) + "\n" + xmlll.GetComponent<XmlReader>().getXmlValue(0, 0, "titre", 1);
    }
}
